# Devlog - Captures Folder Placeholder

## Goal

Prepare a stable folder for future Unity screenshots and gameplay GIFs.

## What changed

- Added `Docs/Captures/.gitkeep` so the captures folder exists before media files are recorded.
- Added `Docs/Captures/README.md` with recommended filenames and README embed snippets.
- Updated `Docs/ManualValidationAndCaptureGuide.md` to mention the prepared captures folder.
- Updated README to list `Docs/Captures/` as the README screenshot/GIF location.

## Why it helps

The user can now save portfolio evidence directly into a known folder after running Unity manual validation. This avoids scattered screenshots and makes future README updates easier.

## Manual follow-up

After Unity testing, place captured files in:

```text
Docs/Captures/
```

Recommended first proof file:

```text
Docs/Captures/04_result_summary_rank.png
```
